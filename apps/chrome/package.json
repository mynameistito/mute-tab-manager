{
  "name": "@mute-tab/chrome",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:watch": "bun --watch run scripts/build.ts",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsgo --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mute-tab/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "*",
    "@types/chrome": "*",
    "ultracite": "*"
  }
}
