---
"@mute-tab/firefox": patch
---

Fix Firefox manifest validation errors: add `background.scripts` fallback alongside `service_worker`, and add `data_collection_permissions` to `browser_specific_settings.gecko`.
