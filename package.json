{
  "name": "mute-tab-manager",
  "description": "Mute/unmute tabs across Chrome and Firefox with proper YouTube support.",
  "version": "0.5.1",
  "type": "module",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build && wxt build -b firefox",
    "build:chrome": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "zip": "wxt zip && wxt zip -b firefox",
    "typecheck": "tsc --noEmit",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "generate-key": "bun run scripts/generate-key.ts",
    "generate-icons": "bun run scripts/generate-icons.ts",
    "release": "bun run scripts/release.ts",
    "changeset": "changeset",
    "version": "changeset version",
    "postinstall": "wxt prepare",
    "prepare": "lefthook install",
    "security:check": "bun scripts/security-check.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@changesets/cli": "^2.31.0",
    "@resvg/resvg-js": "^2.6.2",
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.1.42",
    "@types/node": "^25.9.1",
    "happy-dom": "^20.9.0",
    "lefthook": "^2.1.9",
    "typescript": "^6.0.3",
    "ultracite": "7.8.1",
    "wxt": "^0.20.26"
  }
}
